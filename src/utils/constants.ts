/**
 * Shared application constants.
 * Keep all magic strings and default values here so they stay in sync.
 */

/** Default LM Studio chat-completions endpoint. */
export const DEFAULT_LM_BASE_URL = "http://localhost:1234/v1/chat/completions";

/** Default model placeholder used before auto-detection. */
export const DEFAULT_LM_MODEL = "local-model";
